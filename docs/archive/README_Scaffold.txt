Image UI Slicer — WPF + SkiaSharp Scaffold (v0.2 ABCD)

Open ImageUiSlicer.sln in Visual Studio 2026.
This scaffold wires:
- Export folder: BaseExportFolder\cut outs\
- Naming mode + Prefix UI
- Multi-select + reorder controls
- Nudge placeholder

Next (Copilot):
1) Drag/drop routing per Spec 12
2) CanvasController + lasso tools + commit selection->cutout
3) Real mask-based PNG export
4) Nudge engine hook per Spec 13
